node_modules/
.env
.env.local
*.log
.DS_Store
.~lock.*

# Confidential docs — never commit to public repo
docs/CONFIDENTIAL-*

# Proprietary scoring engine — never commit to public repo
lib/scoring-engine/
